<h1>Editing turma</h1>

<% form_for(@turma) do |f| %>
  <%= render :partial => 'turma_form', :locals => {:f => f} %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @turma %> |
<%= link_to 'Back', turmas_path %>